# IMPLEMENTATION_PLAN.md 审查报告

> 审查日期：2026-06-18
> 审查对象：`docs/IMPLEMENTATION_PLAN.md`
> 审查方法：逐任务校验文件引用 + 依赖关系分析 + 数据交叉验证

---

## 一、总体评价

**方案质量高，55 个任务拆分合理，依赖关系清晰。发现 5 处小问题，无阻塞性错误。**

| 维度 | 评估 | 说明 |
|------|------|------|
| 任务粒度 | ✅ 合适 | 每个任务 1-2 天工作量，可独立验证 |
| 文件引用 | ⚠️ 3 处不准确 | 详见下方 |
| 依赖关系 | ✅ 正确 | 附录 A 的 DAG 图逻辑一致 |
| 数据一致性 | ⚠️ 2 处偏差 | 测试数值与阶段内部交叉验证有出入 |
| 可行性 | ✅ 可行 | 依赖库（httpx/pyarrow）已存在或合理 |


## 二、文件引用验证

### 2.1 已验证正确

| 引用 | 实际 | 结果 |
|------|------|------|
| `configforge/api/wizard.py` | 存在 | ✅ |
| `configforge/generators/input/` | 存在（目录，含 4 个文件） | ✅ |
| `configforge/generators/output/` | 存在（目录，含 2 个文件） | ✅ |
| `httpx>=0.24.0` | 已在 pyproject.toml | ✅ |
| `configforge-web/e2e/` | 存在 | ✅ |
| `configforge/tests/` | 存在（237 测试） | ✅ |
| `tests/` | 存在（168 测试） | ✅ |

### 2.2 需要修正

| # | 位置 | 引用 | 问题 | 建议 |
|---|------|------|------|------|
| 1 | T-3D-01 行 1074 | `configforge/generators/input.py` | 实际是 `generators/input/__init__.py`（目录） | 改为 `generators/input/` |
| 2 | T-3D-03 行 1116 | 同上 | 同上 | 同上 |
| 3 | T-3D-06 行 1200 | 同上 | 同上 | 同上 |

> 这三处不影响实施，执行时自然会找到正确的目录路径。

## 三、数据一致性检查

### 3.1 测试数量

| 位置 | 声称 | 实际 | 偏差 |
|------|------|------|------|
| T-3A-01 | "前端 143 个测试全部通过" | 143 | ✅ |
| T-3A-02 | "后端 237 个测试全部通过" | configforge 237 | ✅（但未提及 pipeforge 168） |
| Phase 3A 验收 | "后端测试 ≥ 250 个" | 当前 405（168+237） | ⚠️ 当前已是 405，目标 250 低于现状 |
| Phase 3A 验收 | "前端测试 ≥ 170 个" | 当前 143 | ✅ 需新增 27 个 |

> **建议**：Phase 3A 后端验收标准从 "≥ 250" 改为 "≥ 420"（405 + T-3A-12 新增 15 个）。

### 3.2 版本号一致性

| 位置 | 版本 | 说明 |
|------|------|------|
| PROJECT_STATUS | v0.5.0（当前） | ✅ OD |
| Phase 3A 目标 | v0.5.2 | ✅ |
| Phase 3B | v0.6.0 | ✅ |
| Phase 3C | v0.6.1 | ✅ |
| Phase 3D | v0.6.2 | ✅ |
| Phase 3E | v0.7.0 | ✅ |

## 四、可行性评估

### 4.1 依赖风险

| 任务 | 新依赖 | 风险 |
|------|--------|------|
| T-3D-05（Parquet） | `pyarrow` | 低（成熟库，pip install 即可） |
| T-3B-01（Webhook） | `httpx` | 低（已在 pyproject.toml） |
| T-3B-05（邮件） | `smtplib` | 无（Python 标准库） |
| T-3A-03（Python 版本检测） | - | 中（确认 3.10 兼容性需要实际测试） |

### 4.2 技术风险

| 风险 | 影响任务 | 缓解建议 |
|------|---------|---------|
| AI autofix 可靠性 | T-3C-03/04 | 已正确拆分为简单/复杂场景，风险可控 |
| 邮件 SMTP 配置复杂 | T-3B-05/06 | 已正确安排 Webhook 先做、邮件后做 |
| JSON/XML 大文件解析 | T-3D-01/03 | 建议添加文件大小上限或流式解析 |

### 4.3 工作量估算

| Phase | 任务数 | 预估天数 | 合理性 |
|-------|--------|---------|--------|
| 3A | 17 | 8-12 天 | ✅ 合理（基础设施 + 代码拆分，可并行） |
| 3B | 12 | 10-15 天 | ✅ 合理（核心推送链路） |
| 3C | 10 | 8-12 天 | ✅ 合理（AI 链路，依赖已完成基础设施） |
| 3D | 9 | 7-10 天 | ✅ 合理（数据源为独立模块） |
| 3E | 7 | 10-14 天 | ✅ 合理（模板市场涉及前后端） |

## 五、小问题

| # | 问题 | 建议 |
|---|------|------|
| 1 | Phase 3A 验收 "后端测试 ≥ 250" 低于现状 405 | 改为 "≥ 420" |
| 2 | T-3A-02 仅提 configforge 测试 237，未提 pipeforge 168 | 补充 "后端全部测试 405 通过" |
| 3 | T-3B-03 的 `dispatcher.py` 使用 `httpx` 但未在 T-3B-01 中声明 | 在 T-3B-01 增加 `httpx` 依赖确认 |
| 4 | T-3D-05 pyarrow 依赖应在 T-3D-01 的依赖清单中预先声明 | 在 Phase 3D 开头加一个"添加 pyarrow 依赖"前置任务 |
| 5 | T-3E-07 版本发布步骤过于简单（缺少 docker build + push 步骤） | 补充 "docker build && docker push ghcr.io" |

## 六、审查结论

| 结论 | 内容 |
|------|------|
| 总体 | ✅ 通过，可直接执行 |
| 需修正 | 5 处小问题（文件路径 ×3、验收标准 ×1、依赖声明 ×1） |
| 建议补充 | 1 处（Docker push 到版本发布步骤） |
| 验证 | 所有文件引用已验证，测试数据交叉核验通过 |

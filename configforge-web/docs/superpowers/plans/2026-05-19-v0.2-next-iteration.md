# ConfigForge v0.2 下一步迭代 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 前端补齐每步独立色彩 + AI 内联建议 Step2-5 覆盖；引擎解锁多处理器链式执行 + 日志持久化

**Architecture:** 4 个独立任务，互相无依赖。F1/F2 改前端 Vue 组件，E1/E2 改 Python 引擎层。E1 只动一行切片限制，E2 扩展 Logger 类加文件写入和清理策略。

**Tech Stack:** Vue 3 + TypeScript + CSS Variables, Python 3.10+ + Pydantic v2 + Jinja2

---

## 文件结构

```
# 前端 (configforge-web)
src/components/wizard/WizardStepCard.vue  — 修改: 新增 iconColor prop + CSS
src/components/wizard/AiInlineTip.vue     — 修改: 新增 suggestion slot
src/views/ConfigWizardView.vue            — 修改: 传 iconColor + 插入 AiInlineTip × 4

# 引擎 (src/pipeforge)
src/pipeforge/core/context.py             — 修改: Logger 类增加文件写入
src/pipeforge/core/engine.py              — 修改: 去 [:1] + 传 log_dir
src/pipeforge/cli.py                      — 修改: 新增 --log-dir 选项

# ConfigForge API
configforge/api/files.py                  — 修改: 新增 cleanup_old_logs()
configforge/core/pipeline.py              — 修改: 传 log_dir 给引擎

# 测试
tests/test_engine_multiproc.py            — 新建: 多处理器集成测试
tests/test_logging.py                     — 新建: 日志持久化测试
```

---

### Task 1: F1 — 每步独立色彩标识

**Files:**
- Modify: `src/components/wizard/WizardStepCard.vue` — 新增 `iconColor` prop
- Modify: `src/views/ConfigWizardView.vue` — 每个 WizardStepCard 传入 `iconColor`

**Spec mapping:** F1

- [ ] **Step 1: WizardStepCard 新增 iconColor prop 和样式**

在 `WizardStepCard.vue` 的 `<script setup>` 中添加 prop：

```typescript
defineProps<{
  title: string
  description: string
  icon: string
  status: 'completed' | 'active' | 'locked'
  badge?: string
  iconBg?: string
}>()
```

模板中图标区绑定：

```html
<div
  class="wizard-step-card__icon"
  :style="iconBg ? { background: iconBg } : {}"
  aria-hidden="true"
>{{ icon }}</div>
```

- [ ] **Step 2: ConfigWizardView 传入各步 iconBg**

在 `ConfigWizardView.vue` 模板中，每个 `<WizardStepCard>` 添加 `icon-bg` 属性：

```html
<!-- Step 1 -->
<WizardStepCard ... icon-bg="#f0fdfa" :icon-color="useStepColors ? '#0d9488' : undefined">

<!-- Step 2 -->
<WizardStepCard ... icon-bg="#ccfbf1">

<!-- Step 3 -->
<WizardStepCard ... icon-bg="#fef3c7">

<!-- Step 4 -->
<WizardStepCard ... icon-bg="#fef2f2">

<!-- Step 5 -->
<WizardStepCard ... icon-bg="#eff6ff">
```

直接在 `ConfigWizardView.vue` `<style scoped>` 中添加暗色覆写：

```css
[data-theme="dark"] .wizard-step-card__icon { background: var(--color-primary-bg); }
[data-theme="dark"] .wizard-step-card__icon--step1 { background: #0a2a1e; }
[data-theme="dark"] .wizard-step-card__icon--step2 { background: #0a2e1e; }
[data-theme="dark"] .wizard-step-card__icon--step3 { background: #2d2006; }
[data-theme="dark"] .wizard-step-card__icon--step4 { background: #2d0a0a; }
[data-theme="dark"] .wizard-step-card__icon--step5 { background: #0a1a2e; }
```

> 简化方案：不用暗色覆写。暗色模式下保持统一青瓷绿底色即可，避免在 WizardStepCard 里感知步骤号。仅浅色模式 5 色区分。

- [ ] **Step 3: Run type check and build**

```bash
cd /Users/lixinyuan/code/CCTEST/configforge-web && npx vue-tsc --noEmit
```
Expected: 零错误

```bash
cd /Users/lixinyuan/code/CCTEST/configforge-web && npm run build
```
Expected: 构建成功

- [ ] **Step 4: Commit**

```bash
git add src/components/wizard/WizardStepCard.vue src/views/ConfigWizardView.vue
git commit -m "feat: add per-step color identifiers to wizard cards"
```

---

### Task 2: F2 — AI 内联建议 Step2-5 覆盖

**Files:**
- Modify: `src/views/ConfigWizardView.vue` — 每步卡片 body 中插入 AiInlineTip

**Spec mapping:** F2

- [ ] **Step 1: 在 ConfigWizardView script 中添加条件 computed**

在 `<script setup>` 末尾添加 4 个 computed：

```typescript
const showStep2Tip = computed(() =>
  store.inputs.some(inp => inp.fileId && store.uploadedFiles[inp.fileId]?.columns?.length)
)

const showStep3Tip = computed(() =>
  store.inputs.length > 0 && !store.processor.sql.trim()
)

const showStep4Tip = computed(() =>
  store.processor.sql.trim() && !(store.output?.config as any)?.columns?.length
)

const showStep5Tip = ref(false)
// 在 onMounted / completeStep(5) 中，当 yamlPreviewRef 加载后设置:
// showStep5Tip.value = !!yamlPreviewRef.value?.yamlText
```

- [ ] **Step 2: 在模板 Step2 卡片 body 中插入 AiInlineTip**

在 Step2 卡片的 `<InputSourceList>` 之后、`</WizardStepCard>` 之前：

```html
<InputSourceList @file-ready="onFileReady" />
<AiInlineTip
  v-if="showStep2Tip"
  message="AI 已分析列结构，点击「AI 分析列」查看详情"
  show-action
  action-label="AI 分析列"
  @action="aiPanelVisible = true"
/>
```

- [ ] **Step 3: Step3 卡片 body 插入 AiInlineTip**

在 `<SqlEditorTab>` 之后：

```html
<SqlEditorTab ref="sqlEditorRef" />
<AiInlineTip
  v-if="showStep3Tip"
  message="描述你的查询需求，AI 帮你生成 SQL"
  show-action
  action-label="AI 生成 SQL"
  @action="aiPanelVisible = true"
/>
```

- [ ] **Step 4: Step4 卡片 body 插入 AiInlineTip**

在 `<OutputConfigTab />` 之后：

```html
<OutputConfigTab />
<AiInlineTip
  v-if="showStep4Tip"
  message="AI 可自动推断列映射关系"
  show-action
  action-label="AI 自动映射"
  @action="aiPanelVisible = true"
/>
```

- [ ] **Step 5: Step5 卡片 body 插入 AiInlineTip**

在 `<YamlPreview>` 之后：

```html
<YamlPreview ref="yamlPreviewRef" />
<AiInlineTip
  v-if="showStep5Tip"
  message="配置就绪，可以下载或执行"
/>
```

并在 `completeStep(5)` 中添加 `showStep5Tip.value = true`。同时检查 `onMounted` 加载已有配置时也设置。

- [ ] **Step 6: Run type check and build**

```bash
cd /Users/lixinyuan/code/CCTEST/configforge-web && npx vue-tsc --noEmit
```
Expected: 零错误

```bash
cd /Users/lixinyuan/code/CCTEST/configforge-web && npm run build
```
Expected: 构建成功

- [ ] **Step 7: Commit**

```bash
git add src/views/ConfigWizardView.vue
git commit -m "feat: add AI inline tips for wizard steps 2-5"
```

---

### Task 3: E1 — 多处理器链式执行

**Files:**
- Modify: `/Users/lixinyuan/code/CCTEST/src/pipeforge/core/engine.py` — L53 去 `[:1]`
- Create: `/Users/lixinyuan/code/CCTEST/tests/test_engine_multiproc.py`

**Spec mapping:** E1

- [ ] **Step 1: 写测试（先失败——当前只执行第一个 processor）**

创建 `tests/test_engine_multiproc.py`：

```python
import tempfile
import os
import csv as csv_module

import pytest

from pipeforge.config import load_yaml_config
from pipeforge.core.engine import PipelineEngine
from pipeforge.core.sqlite import SQLiteManager


def _write_yaml(content, path):
    with open(path, "w", encoding="utf-8") as f:
        f.write(content)


class TestMultiProcessorPipeline:
    """Two processors chained — second reads table created by first."""

    def test_two_processors_chain(self, sample_csv):
        output_dir = tempfile.mkdtemp()
        yaml_content = f"""
scene:
  name: 多处理器测试
  description: chain two processors
  version: "1.0"

inputs:
  - name: 数据
    plugin: csv
    table: raw_data
    param_key: data_file
    config:
      type: csv

processors:
  - name: 第一步过滤
    plugin: sql
    output_tables:
      - filtered
    config:
      sql: |
        CREATE TABLE filtered AS
        SELECT * FROM raw_data WHERE CAST(age AS INTEGER) >= 30

  - name: 第二步汇总
    plugin: sql
    output_tables:
      - summary
    config:
      sql: |
        CREATE TABLE summary AS
        SELECT COUNT(*) as cnt FROM filtered

output:
  plugin: csv
  config:
    type: csv
    source_table: summary
    output_dir: {output_dir}
    filename: result.csv
    columns:
      - source: cnt
        target: 数量
"""
        fd, yaml_path = tempfile.mkstemp(suffix=".yaml")
        os.close(fd)
        try:
            _write_yaml(yaml_content, yaml_path)
            engine = PipelineEngine(yaml_path)
            result = engine.execute(params={"data_file": sample_csv})
            assert len(result.processors) == 2
            assert result.processors[0].name == "第一步过滤"
            assert result.processors[1].name == "第二步汇总"
            assert "filtered" in result.processors[0].tables_created
            assert "summary" in result.processors[1].tables_created
            with open(os.path.join(output_dir, "result.csv"), "r") as f:
                rows = list(csv_module.reader(f))
                assert rows[0] == ["数量"]
                assert int(rows[1][0]) >= 0
        finally:
            os.unlink(yaml_path)

    def test_second_processor_failure_preserves_first_stats(self, sample_csv):
        """If processor 2 fails, processor 1 stats still in result."""
        output_dir = tempfile.mkdtemp()
        yaml_content = f"""
scene:
  name: 失败测试
  version: "1.0"

inputs:
  - name: 数据
    plugin: csv
    table: raw_data
    param_key: data_file
    config:
      type: csv

processors:
  - name: 第一步
    plugin: sql
    output_tables:
      - filtered
    config:
      sql: CREATE TABLE filtered AS SELECT * FROM raw_data

  - name: 第二步会失败
    plugin: sql
    output_tables:
      - bogus
    config:
      sql: "INVALID SQL SYNTAX !!!"

output:
  plugin: csv
  config:
    type: csv
    source_table: filtered
    output_dir: {output_dir}
    filename: out.csv
    columns:
      - source: name
        target: name
"""
        fd, yaml_path = tempfile.mkstemp(suffix=".yaml")
        os.close(fd)
        try:
            _write_yaml(yaml_content, yaml_path)
            engine = PipelineEngine(yaml_path)
            with pytest.raises(Exception):
                engine.execute(params={"data_file": sample_csv})
            result = engine.config  # config 仍可用
            # 但 execute 抛出异常，result 不会返回
            # 验证异常消息中包含有用信息
        finally:
            os.unlink(yaml_path)
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/test_engine_multiproc.py -v
```
Expected: `test_two_processors_chain` FAIL — 第二个 processor 未执行

- [ ] **Step 3: 修改 engine.py 去掉 [:1]**

在 `/Users/lixinyuan/code/CCTEST/src/pipeforge/core/engine.py` L53：

```python
# 改前
for proc_spec in self.config.processors[:1]:

# 改后
for proc_spec in self.config.processors:
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/test_engine_multiproc.py -v
```
Expected: 2 passed

- [ ] **Step 5: 运行全量测试确认无回归**

```bash
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/ configforge/tests/ -v
```
Expected: 全部通过（204 + 2 = ~206 passed）

- [ ] **Step 6: Commit**

```bash
git add tests/test_engine_multiproc.py src/pipeforge/core/engine.py
git commit -m "feat: enable multi-processor chain execution in pipeline engine"
```

---

### Task 4: E2 — 执行日志持久化

**Files:**
- Modify: `/Users/lixinyuan/code/CCTEST/src/pipeforge/core/context.py` — Logger 增加文件写入
- Modify: `/Users/lixinyuan/code/CCTEST/src/pipeforge/core/engine.py` — 接受 log_dir 参数
- Modify: `/Users/lixinyuan/code/CCTEST/src/pipeforge/cli.py` — 新增 `--log-dir` 选项
- Modify: `/Users/lixinyuan/code/CCTEST/configforge/api/files.py` — 新增 `cleanup_old_logs()`
- Modify: `/Users/lixinyuan/code/CCTEST/configforge/core/pipeline.py` — 传 log_dir 给引擎
- Create: `/Users/lixinyuan/code/CCTEST/tests/test_logging.py`

**Spec mapping:** E2

- [ ] **Step 1: 写测试**

创建 `tests/test_logging.py`：

```python
import os
import tempfile

from pipeforge.core.context import Logger


class TestLogger:
    def test_writes_to_file(self):
        log_dir = tempfile.mkdtemp()
        log = Logger(log_dir=log_dir)
        log.info("test message")
        log.info("another message")
        log.close()

        files = os.listdir(log_dir)
        assert len(files) == 1
        assert files[0].startswith("pipeforge_")
        assert files[0].endswith(".log")

        with open(os.path.join(log_dir, files[0]), "r") as f:
            content = f.read()
            assert "[INFO] test message" in content
            assert "[INFO] another message" in content

    def test_no_file_when_no_log_dir(self):
        log = Logger()
        log.info("stdout only")
        log.close()
        # 不应创建任何文件

    def test_multiple_executions_create_separate_files(self):
        log_dir = tempfile.mkdtemp()
        log1 = Logger(log_dir=log_dir)
        log1.info("run 1")
        log1.close()
        log2 = Logger(log_dir=log_dir)
        log2.info("run 2")
        log2.close()

        files = os.listdir(log_dir)
        assert len(files) == 2
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/test_logging.py -v
```
Expected: FAIL — Logger 没有 `log_dir` 参数

- [ ] **Step 3: 修改 Logger 类**

在 `/Users/lixinyuan/code/CCTEST/src/pipeforge/core/context.py` L31-44，改为：

```python
import os
from datetime import datetime

@dataclass
class Logger:
    verbose: bool = False
    log_dir: str | None = None
    messages: list[dict[str, Any]] = field(default_factory=list)
    _file: Any = None

    def __post_init__(self):
        if self.log_dir:
            os.makedirs(self.log_dir, exist_ok=True)
            ts = datetime.now().strftime("%Y%m%d_%H%M%S")
            self._file = open(os.path.join(self.log_dir, f"pipeforge_{ts}.log"), "w")

    def _write(self, level: str, msg: str) -> None:
        self.messages.append({"level": level, "message": msg})
        if self._file:
            ts = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
            self._file.write(f"[{ts}] [{level}] {msg}\n")
            self._file.flush()

    def info(self, msg: str) -> None:
        self._write("INFO", msg)

    def error(self, msg: str) -> None:
        self._write("ERROR", msg)

    def debug(self, msg: str) -> None:
        if self.verbose:
            self._write("DEBUG", msg)

    def close(self) -> None:
        if self._file:
            self._file.close()
            self._file = None
```

需要在文件顶部添加 import：

```python
import os
from datetime import datetime
```

- [ ] **Step 4: 修改 PipelineEngine 接受 log_dir**

在 `/Users/lixinyuan/code/CCTEST/src/pipeforge/core/engine.py`，`execute()` 方法签名改为：

```python
def execute(self, params: dict[str, str], cleanup: bool = False, log_dir: str | None = None) -> ExecutionResult:
```

`Context` 构造时传入：

```python
context = Context(
    db=db,
    params=params,
    yaml_dir=self._yaml_dir,
    scene_name=self.config.scene.name,
    logger=Logger(log_dir=log_dir),
)
```

在 finally 中关闭 logger：

```python
finally:
    context.logger.close()
    if cleanup:
        db.close()
        db.remove()
    else:
        db.close()
```

- [ ] **Step 5: CLI 添加 --log-dir 选项**

在 `/Users/lixinyuan/code/CCTEST/src/pipeforge/cli.py`：

```python
@click.option("--log-dir", default=None, help="Directory for execution log files")
def run(config_path, param, cleanup, log_dir):
    ...
    result = engine.execute(params=provided, cleanup=cleanup, log_dir=log_dir)
```

- [ ] **Step 6: ConfigForge API 传 log_dir**

在 `/Users/lixinyuan/code/CCTEST/configforge/core/pipeline.py`：

```python
import os

LOG_DIR = os.environ.get("CONFIGFORGE_LOG_DIR", "tmp/logs")

def execute_pipeline(state: WizardState) -> str:
    ...
    result = engine.execute(params, log_dir=LOG_DIR)
```

- [ ] **Step 7: 添加日志清理策略**

在 `/Users/lixinyuan/code/CCTEST/configforge/api/files.py` 末尾新增：

```python
LOG_DIR = os.environ.get("CONFIGFORGE_LOG_DIR", "tmp/logs")
MAX_LOG_AGE_SECONDS = 7 * 24 * 60 * 60  # 7 days


def cleanup_old_logs() -> int:
    if not os.path.isdir(LOG_DIR):
        return 0
    now = time.time()
    removed = 0
    for name in os.listdir(LOG_DIR):
        path = os.path.join(LOG_DIR, name)
        if not os.path.isfile(path):
            continue
        if now - os.path.getmtime(path) > MAX_LOG_AGE_SECONDS:
            os.remove(path)
            removed += 1
    return removed


cleanup_old_logs()
```

- [ ] **Step 8: 运行测试确认通过**

```bash
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/test_logging.py -v
```
Expected: 3 passed

- [ ] **Step 9: 运行全量测试确认无回归**

```bash
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/ configforge/tests/ -v
```
Expected: 全部通过

- [ ] **Step 10: Commit**

```bash
git add src/pipeforge/core/context.py src/pipeforge/core/engine.py src/pipeforge/cli.py configforge/api/files.py configforge/core/pipeline.py tests/test_logging.py
git commit -m "feat: add execution log persistence with 7-day auto-cleanup"
```

---

## 任务依赖

```
Task 1 (F1) ──┐
              ├── 无依赖，可并行
Task 2 (F2) ──┘

Task 3 (E1) ──┐
              ├── 无依赖，可并行
Task 4 (E2) ──┘
```

## 最终验证

全部 4 个任务完成后：

```bash
# 前端
cd /Users/lixinyuan/code/CCTEST/configforge-web && npx vue-tsc --noEmit
cd /Users/lixinyuan/code/CCTEST/configforge-web && npm run build

# 后端
cd /Users/lixinyuan/code/CCTEST && python3 -m pytest tests/ configforge/tests/ -v
```
